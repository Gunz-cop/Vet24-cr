# Implementación local I01 / #27

Se extrajo el esquema de clínicas a `src/lib/clinic-schema.ts` y se añadió `schedule_v1` opcional con validación estricta. El motor puro opera en minutos y UTC−6, distingue desconocido/cita/cerrado, aplica excepciones por fecha y divide cruces de medianoche con confirmación explícita cuando el día siguiente es desconocido.

El parser legado rechaza horas imposibles, días omitidos y segmentos relevantes sin interpretar. Los serializadores producen texto, compatibilidad compacta y Schema.org desde el mismo modelo. La comprobación de emergencias 24/7 requiere capacidades y evidencia directa, además de calendario completo sin excepciones incompatibles. No se modificaron fichas; su saneamiento pertenece a #29 y la migración de consumidores a #32.

Validación: 11 pruebas focalizadas aprobadas, incluyendo tres zonas horarias y los 1440 minutos del adaptador compacto. Auditoría independiente Luna medium: `auditoria-i01-r2.md`, APROBADO. Corrige el P1 de r1 relativo a excepciones de cierre. También se corrigieron errores de coma flotante en la representación compacta.

La suite conjunta y el bundle se documentan por separado al concluir la integración con I04. No se publicó ni se hizo commit.
